# Downtime log

Small internal tool for Anand Precision Works, Chakan plant. Operators log machine
stoppages from a tablet on the shop floor; supervisors review totals per shift.

    npm install
    npm start

Open http://localhost:3000. Sign in as `operator1` or `supervisor` (password `welcome123`).

Built in a weekend. Not reviewed by anyone.
